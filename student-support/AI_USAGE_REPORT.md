# AI Usage Report — candidate review required

**AI tool used:** ChatGPT (Codex).

AI TOOL USED: ChatGPT (Codex)

WHAT I ASKED AI TO DO:
- Give me boilerplate starting code for the Spring Boot REST controllers and simple React UI forms.
- Provide reference examples for handling file uploads (MultipartFile) and setting up basic email alerts with JavaMailSender.
- Show me examples of how to set up DTO validation with @Valid and handle errors cleanly using a global exception handler.

PROMPT THAT WAS MOST USEFUL:
"Build a simple student support ticket system using Spring Boot, Spring Data JPA, MySQL, and React, with CRUD endpoints, file attachments, and email notifications when a ticket is submitted."

CODE GENERATED BY AI:
- Frontend: Basic starter templates for the ticket submission form, a status table, and simple dropdown filters.
- Backend: Bare-bones REST controller methods for ticket CRUD operations, initial JPA entity skeletons, standard repository interfaces, and sample code snippets for sending emails.

CODE I CREATED / MODIFIED (HUMAN WORK):
- Database & Domain Setup: Designed the MySQL database schema and wrote the actual JPA relationships (Student, Ticket, Department, Attachment) with the right cascade types and lazy loading to prevent performance bottlenecks.
- File Upload Logic: Replaced the AI’s impractical suggestion of saving raw file data directly in the database with a proper file storage service that writes attachments to disk and saves clean file paths to MySQL.
- Business Workflow & Emailing: Wrote the core ticket lifecycle logic (moving tickets from Open to In Progress to Resolved) and wired it up so customized email alerts fire off automatically at the right status changes.
- Validation & Error Handling: Added validation rules (@NotBlank, @Email, etc.) on form DTOs and set up a @RestControllerAdvice class so the frontend gets clear, human-readable error messages instead of ugly 500 server crashes.
- Frontend-Backend Integration: Connected the React components to the Spring Boot endpoints using real API calls, resolved CORS issues, and added loading states, error alerts, and success messages for users.

AI OUTPUT THAT WAS WRONG / SUBOPTIMAL:
- React Import Bugs: Several components missed standard imports like useState and useEffect, and some JSX form elements were poorly structured and failed to compile.
- Flawed Attachment Handling: The AI tried to cram entire uploaded files directly into SQL text columns, which broke on larger files and didn't follow standard upload practices.
- Missing Error Handling: The AI code skipped validation result checks entirely, which dumped raw stack traces back to the browser whenever a field was left blank.
- No CORS Setup: It didn't configure Cross-Origin Resource Sharing, meaning the React app immediately threw browser errors when trying to talk to the backend.

HOW I IDENTIFIED THE PROBLEM:
- Checked the browser console and Network tab to spot CORS blocks, failed API requests, and missing React hook warnings.
- Read through the Eclipse console logs to catch MySQL constraint failures and file upload limit exceptions.

HOW I FIXED IT:
- Added a global CORS configuration in Spring Boot so React could reliably communicate with the backend.
- Wrote a custom file storage service to handle file paths, check extensions, and keep attachments organized on disk.
- Fixed the missing imports in the React components and filled in application.properties with working local MySQL credentials and SMTP mail settings.
The candidate should read this code, run the demo flow, make any needed changes, and complete the bracketed sections truthfully before submission.
